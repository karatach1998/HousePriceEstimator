apiVersion: v1
kind: Service
metadata:
  name: model-server
spec:
  type: LoadBalancer
  selector:
    app: model-server
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http