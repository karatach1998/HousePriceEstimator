apiVersion: v1
kind: Service
metadata:
  name: model_server
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    matchLabels:
      app: model_server