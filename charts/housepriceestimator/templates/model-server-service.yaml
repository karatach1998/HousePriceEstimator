apiVersion: v1
kind: Service
metadata:
  name: model-server
spec:
  type: ClusterIP
  selector:
    app: model-server
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http